// method chaining = calling one method after 
//                   another in one countinous line of code

let userName = "GALANTIXA";

let letter = userName.charAt(0);
letter = letter.toLowerCase();

console.log(letter);